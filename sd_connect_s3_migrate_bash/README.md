### Description
Migration CLI MVP tool implemented with `sh`, that uses `rclone` and `sd-lock-util` to automate the bucket migration.
Additionally the tool uses `transliterate` from node side for automating the bucket name conversion.
