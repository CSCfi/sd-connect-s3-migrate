[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sd-connect-s3-migrate-cli"
version = "0.1.0"

[tool.hatch.build.targets.wheel]
packages = ["sd_connect_s3_migrate_cli"]
